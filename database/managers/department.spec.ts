import DepartmentManager from "./department"
import DepartmentFactory from "~/factories/department"

describe("Database: Department Read Operations", () => {
	it.todo("can count single department")

	it.todo("can count single department with zero users")

	it.todo("can count multiple departments")
})
